[package]
name = "gamer-keymap-fixture"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
wit-bindgen = "0.57.1"
wit-component = "0.254.0"
# profile 消费：keymap YAML（schema 同 data/<pkg>/keymap）在 guest 内解析
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
