# 项目目录清理报告

日期：2026-09-20。范围：D:\code\gamer。

## 后续：用户确认开发测试备份全部可删除

用户随后明确说明上述备份与历史数据均为开发测试数据，并授权清理。本轮又删除 16 个目标、598 个文件，实际释放约 1.51 GiB，无失败项：

- backups、baseline-backups、插件 ID 转换前的完整数据备份、失败转换临时目录、server/data/_migration_backup。
- pre-plugin-split-20260920（含 working-tree.patch）、旧服务程序备份、release/dist 历史发行与测试数据。
- 隔离浏览器 / YAML 验收实例的数据、临时配置与密码、旧静态产物和演示归档；测试脚本、结果日志与截图保留。

此前被审批拦截的失败转换目录，本轮在用户明确授权后已成功删除；docker-data 核对时也已不存在。以上更新覆盖下文首次清理时的“保留”状态。当前 server/data（除旧迁移备份）、server/config.toml、源码与未提交改动保留，前后端检查均为 HTTP 200。清理明细在 server/target/cleanup-approved-backups-results.json。

## 扫描与清理结果

扫描了包含隐藏文件、忽略文件在内的目录树，统计约 127,506 个文件、112.27 GiB 文件长度。不跟随符号链接或目录联接，不修改 Git 内部数据。结合构建入口、源码引用、运行进程和现有验收记录判断清理范围；没有仅凭“没有静态 import”删除插件动态入口、测试夹具或类型声明。

本次删除 **50,864 个文件，文件长度合计约 89.22 GiB**；分批测量的磁盘可用空间增加合计约 **82.98 GiB**，该值未计入小型临时文件清理。文件长度与实际释放空间不同，构建文件的硬链接、文件系统分配方式及同期写入都会影响统计。

清理后重新扫描约 76,655 个文件、23.07 GiB 文件长度。扫描及报告本身也会产生少量文件，因此数量不是固定发行指标。

| 已清理内容 | 判断依据 |
| --- | --- |
| server/guests 与 tools/plugins 的遗留内容，以及旧插件空源码目录 | 源码和 manifest 已迁移到 plugins/gamer-*；旧路径仅剩缓存或空目录 |
| server/target-phase3、target-rc、旧交叉编译与 guest 缓存 | 当前未使用的构建产物，后续相应命令可重新生成 |
| 服务端及 launcher 增量编译缓存 | 可重建，不包含源代码；服务端此项约 53 GiB |
| 旧服务端 / 测试可执行文件、依赖描述和 540 个剩余编译中间文件 | 保留当前服务二进制及最新测试程序，清除其他旧构建产物 |
| 服务端及 launcher 的旧 Release 构建目录 | 产物分别停留在 9 月 8 日和 9 月 6 日，与本次重构后的源代码不一致，且没有运行进程使用 |
| 旧浏览器测试配置目录、9 月 10 日临时验收环境、较早的插件 UI 演练副本 | 一次性验证产物；保留最新成功的浏览器证据与测试工具 |
| 已执行完的一次性改写脚本、Python 字节码、旧误写日志、run-body.json | 不被项目构建或业务引用的临时产物 |

另移除了 architecture_guard_tests.rs 中针对已迁移插件旧目录的失效豁免，更新其路径注释，避免未来在旧目录新增业务代码时被守卫漏检。

## 保留内容

- 当前 server/data、本机配置、完整数据转换备份、backups、baseline-backups、docker-data 及未提交源码修改。
- release/dist 内发现了实际脚本、模板和数据库，因此没有把整个发行目录当成可丢弃缓存删除。
- 当前服务二进制、最新测试程序、现用依赖缓存、node_modules、server/web-dist、官方插件归档和 UI 产物；这些文件用于当前运行、开发或发行。
- 测试脚本、夹具、许可证、SDK 示例、历史设计与验收文档，以及最新 UI 更新验收证据。缺少业务调用不代表这些开发文件无用。
- 本次执行清单与统计留在 server/target/cleanup-*.json 和日志中，用于核对清理结果；均不进入版本控制。
- 失败转换目录 server/data.plugin-convert-20260920-101619-855823 仍保留。用户本次要求清理后，再次尝试删除该明确目录，自动审批仍拒绝，理由仅为 blocked by policy；未绕过或改用其他工具重试该目录。

## 验证与影响

- 修改后的 7 项架构守卫测试全部通过。
- 8443 服务健康检查返回 200；5173 使用浏览器 HTML 请求返回 200，页面标题为 Gamer。
- 清理执行成功，没有文件占用导致的部分删除；失败转换目录的工具审批拒绝单独记录，不计入已删除统计。
- Git 差异空白检查通过；Windows CRLF 启动脚本按其既有编码规则校验。
- 未停止当前服务，未操作用户设备，未进行 Git 提交。
- 后续首次重新编译 Rust 或构建 Release 需要重建已清理的缓存，耗时可能增加；当前运行和源码不受影响。
