# Gamer 界面剩余事项处理报告

日期：2026-09-19。承接 [重写报告第 10 节](gamer-ui-rewrite-report.md) 已获认可的处理建议。本报告区分代码完成、自动化验证、实际浏览器验证和外部条件限制；原报告的“尚未实现”描述以本报告为最新状态。

## 1. 本轮结果

保留已经确定的黑灰黄主题、48px 单行品牌导航、投屏上方两行工具条、可拖动左右区、28px 紧凑控件、内联编辑和底部轻状态条。没有重新设计页面结构。

| 原缺项 | 已落地结果 | 仍需注意 |
| --- | --- | --- |
| 拆分函数库不可编辑 | 所有 `_function*.yaml` 选择后进入同一画布，按真实定义文件保存、删除和重命名；版本冲突保留既有处理 | 跨文件引用不自动联动修改，仍被引用时拒绝提交并指出文件 |
| 跨函数报错位置不可靠 | 解释器→WASM 宿主→运行事件→前端携带运行 ID、调用帧、资源三元组和执行版本；保留最内层失败 | 没有保存完整历史源码；源版本变化时展示身份与差异，不强行高亮 |
| iframe 缺少可复用主题 | 新增框架无关 UI 客户端、CSS、示例及构建工具；实际安装沙盒示例验证 | 第三方需要主动采用，不由宿主穿透改样式 |
| 部分管理反馈重复/陈旧 | 模板上传、替换、匹配、配置包管理及系统检查/下载/安装成功等接入当前操作反馈；操作序号隔离旧结果 | 表单校验仍在原处，关键升级失败仍留确认弹窗；不是删除所有 toast |
| 录制历史依赖素材推断 | Core 增加真实会话列表；视频显示会话、分段、事件数、缺失素材，含没有素材的取消/失败记录 | 历史登记不等于事件备份，见第 5 节 |
| 旧图标/控件不一致 | 素材、模板、任务、映射、系统主要按钮改用同一 SVG 图标；补图标按钮名称 | 仍有少量字符分隔、拖拽手柄、业务专用样式，未做无收益的全库重造 |
| 首次下载过大 | 业务面板、市场、插件中心与报错预览按需加载，插件中心关闭时不加载 | Console 仍超过 500 KB；未提高警告阈值来隐藏问题 |
| 验收不足 | 全量前后端测试、解释器/真实 WASM、隔离 API、浏览器三尺寸及 100/500 步检查 | 真机输入、多个浏览器及长时间内存观测仍未完成 |

## 2. 关键实现与设计修正

### 函数编辑

函数下拉继续以函数为主，仅用轻量来源提示区分文件；不让用户先找库文件再找函数。更新/删除先读取目标文件的当前内容，并携带其 `expected_version` 写回；删除最后一个函数保存 `functions: {}`，不会直接删除整个文件。新函数默认进入 `_function.yaml`，自动命名检查当前包所有库。

gamer.yaml 保存钩子负责原生函数/其他库同名检查，以及删除/改名的剩余引用检查。引用尚未修复返回 `yaml.functions.referenced`；其他文件无法解析而无法确认引用时明确拒绝。该机制在插件内，Core 不解释函数名。它不是跨文件事务或批量重构功能；直接通过通用资源 DELETE 删除整个库也不属于这次函数编辑保护路径。

### 报错定位

每次调用有独立 `frame_id` 和 `parent_frame_id`，来源取执行所用的资源快照，包含 Package、Plugin、路径和内容版本。前端保存首次最内层失败，避免函数退栈时父步骤覆盖真正错误。已有运行事件没有 trace 时仍可看详情。

当前画布与执行来源/版本一致且没有未保存改动时，直接定位；否则打开只读错误位置预览，保留当前草稿。源码已变或被删除时明确解释，不用旧步骤号猜位置。只读预览是诊断补充，函数正常编辑仍然在右侧主画布。现阶段不提供历史源码回放，也不承诺完整调用栈树展示。

### 轻状态条

内核相关操作在左、插件相关操作在右，各自只保留当前结果。模板批量导入会冻结开始时的配置包，切包后后续条目不会误写到新包，旧结果也不覆盖新列表。当前面板身份由宿主验证，插件不能通过消息内容冒充其他插件。

更新策略成功只发布一次结果；校验失败继续留在表单。安装/回滚的断连等待与失败仍沿用原确认流程，不能用一条短状态替代必须处理的错误。没有增加保存/运行/停止重复按钮或全局消息历史。

### iframe 接入

初版外链模块示例在实际沙盒内不能工作；最终改成带脚本/样式 SHA-256 CSP 哈希的单文件 HTML。原因是 opaque origin 下子资源不能依赖 Strict 会话 Cookie，ES module 还受跨源约束。另修正原生 form submit 在沙盒中不触发的问题，按钮使用 `type=button` 和 click。保留 `sandbox=allow-scripts`，没有增加 `allow-same-origin/allow-forms`、unsafe-inline 或放宽认证。

源码位于 `sdk/ui/`；执行 `node tools/build-ui-sdk.mjs` 生成示例。实际浏览器已验证主题连接、按钮发布状态、宿主右侧出现复制/详情入口。

### 录制历史

增加已认证的 `GET /api/recording`；历史来自 Core 会话元数据，而非媒体文件名。登记镜像保存到媒体根下独立历史目录，旧录制目录仍可被枚举；活动状态优先，重启后遗留活动记录恢复为 interrupted。列表按开始时间倒序，分段素材删除后标缺失。

视频页消费公开 API，加载失败、空态和读取中分开显示，切换时忽略过期响应。去掉无意义的素材状态筛选（就绪/缺失不是录制会话终态）和草稿页已过时的“后端没有历史 API”提示。

### Windows 保存和打包

全量测试暴露 Windows 文件替换偶发占用：原子替换只对 5/32/33 错误做有限重试，最多 8 次、累计等待 140ms；不先删除目标，持续失败仍上报。另将 YAML manifest 常量改为直接引用打包源，避免插件版本和换行漂移。修正一条过时测试：`wait_find` 默认值以现有原生注册表的 3s 为准，没有改回 30s。

## 3. 验收结果与证据

| 层次 | 本轮结果 |
| --- | --- |
| 前端全量 | 885 项通过；包含架构边界、拆分库保存、过期状态、首个失败保留、版本变化拒绝误定位、SDK 端口协议、真实历史 UI 等 |
| Rust 全量 | 661 项通过、0 失败、6 项既有 ignored；未新增跳过项 |
| 解释器 | 15 项通过；覆盖中文函数内失败的资源/调用帧与父调用退栈 |
| 真实 WASM | 构建并组件化新 YAML guest；运行事件测试额外断言 trace 可穿过 WASM→宿主事件链 |
| 隔离 API | 6 项真实检查：3.1.2 安装/更新、分库写回与版本冲突、重复/引用拒绝、空库保存、性能资源创建、无素材取消会话列表 |
| 构建 | Vue 生产构建与 Rust 隔离输出构建通过；gamer.yaml 3.1.2 归档打包、校验通过 |
| 浏览器 | 分库函数修改/保存/重开；严格沙盒 SDK 连接与状态操作；录制历史和真实会话 ID 进入草稿；无 browser error 日志 |

浏览器视口/DOM 测量：

| 视口 | body.scrollWidth | 投屏容器 | 右侧面板 | 结论 |
| --- | ---: | --- | --- | --- |
| 1920×1080 | 1920 | 约 1034×924 | 约 880×1003 | 单行 48px 头部，并排无整体横向溢出 |
| 1366×768 | 1366 | 约 734×612 | 约 626×691 | 并排布局可用 |
| 800×900 | 800 | 约 792×321 | 约 792×495 | 切换上下布局，无整体横向溢出 |

以上投屏尺寸是容器，不是强制拉伸视频分辨率；真实画面仍按比例居中。隔离环境未连接设备，不能把容器检查算作真实触控/视频检查。

100/500 步脚本均实际加载完整卡片。一次暖缓存“选择→最后一张卡片存在”的工具往返约 194ms/625ms（包含浏览器控制开销，不是输入延迟或严格性能分位数）。没有实现虚拟列表，也没有进行长时间内存增长测量。

主工作台块约 864.85 KB / gzip 364.10 KB → 625.49 KB / gzip 283.04 KB，减少约 27.7% / 22.3%。VideoWorkbench 等分为独立块。剩余 500 KB 警告明确保留；这不是所有浏览器下首屏速度提升的结论。

本地证据位于 `server/target/`：`ui-completion-tests.json/.log`、`ui-completion-rust-tests.log`、`ui-completion-trace-wasm.log`、`ui-completion-build.log`、`ui-completion-backend-build.log`、`ui-completion-api.log`、`yaml-acceptance/completion-results.json`。目标目录是本地生成物；报告保存主要结论，便于清理构建目录后仍可追溯。

## 4. 交付与启用

- 规范：[gamer-ui-spec.md](../design/gamer-ui-spec.md)；SDK：[README](../../sdk/ui/README.md)。YAML/插件 API/插件指南同步补充契约。
- YAML 插件产物：`web/public/plugins/gamer.yaml-3.1.2.gplugin`；本地 registry 已更新该条目。
- 前端产物：`server/web-dist/`；后端可构建源码和隔离验收二进制均已生成。
- 没有提交、推送或发布。没有重启日常 8443/5173 服务或更新其已安装插件；隔离服务使用 18443 和独立数据，ADB 禁用。测试资源只写入隔离目录。
- 日常启用新增后端 API/trace 转发需要在设备/任务空闲时重启新后端，并更新已安装 gamer.yaml 到 3.1.2；只刷新前端不能替换正在运行的服务二进制或旧 guest。

## 5. 仍存在的限制与后续处理

1. **真机未执行**：只读发现设备 25079RPDCC。尚未收到可用于输入测试的应用/页面答复，未向设备发送点击、启动、录制或运行。后续在允许的页面验证连接/接管、1920×1080 坐标及百分比、截图/录制、停止/断连与全屏键盘。
2. **跨文件编辑不是事务重构**：现在拒绝会留下旧引用的改名/删除；跨文件同时并发编辑不属于原子范围。推荐后续仅在确有需要时增加插件拥有的批量重构事务，不在 UI 逐文件部分提交。
3. **报错源历史有限**：只有身份/内容版本，无历史源码副本；文件改动后只给出差异说明。递归/多层条件代码路径支持帧身份，但所有组合的真机错误跳转尚未逐项实测。
4. **历史保留有限**：新登记可独立保留，升级前已经删除的历史无法恢复；旧记录未统一离线回填。事件 JSONL 仍随原媒体目录，缺失会明确报错，不能再生成完整草稿。历史目前无分页/清理功能；记录量大时再增加游标分页与独立清理，不连带删除素材。
5. **环境覆盖有限**：本轮只有当前 Chromium；未宣称 Chrome/Edge/Firefox/Safari 全量或屏幕阅读器/移动触控通过。拖拽排序、分隔限位有自动化覆盖，原生拖拽手感未新增跨浏览器验收。
6. **性能及视觉还有渐进空间**：剩余大块、连续切页内存和少量旧局部样式保留为后续项。优先测量真实使用的脚本规模再决定词库延迟加载或虚拟列表；不以删除功能或提高警告阈值换取表面通过。

本轮能在当前环境中安全完成的实现和验证已完成；上述未验证条件没有被测试替身冒充完成。

最终复核：SDK 内联 script/style 的 CSP 哈希与生成文件逐项匹配，按钮保持 type=button；真实 WASM trace 补充测试 1/1 通过；最终隔离 API 6/6 通过，包为 `ui-completion-1789791351070`。`git diff --check` 通过。隔离 18443 验收服务已停止，数据及日志保留；日常服务未停止。

## 后续调整：投屏状态归入底栏

按用户最新确认，键盘控制启用提示及 FPS / ms / Mbps 移至底栏左侧，用无边框紧凑文字展示。移除投屏键盘高亮边框、画面内键盘徽标和旧统计浮层，工具条不重复显示指标。实时状态与坐标等当前操作反馈独立更新；断开连接清除实时指标，媒体模式不显示键盘已启用。右侧插件反馈和底栏拖拽/裁切/悬停补全保持原交互。相关回归 32 项通过，生产构建通过；本次未新增真机验证。

## 后续调整：状态条功能检查与统一设计

完整功能矩阵及接入规则见 [状态条设计规范](../design/gamer-statusbar-design.md)。底栏保持 29px：左侧当前操作在前、画面指标在后；右侧当前插件的编辑/运行状态。分辨率归入底栏，媒体模式显示素材尺寸和时间；取点、取色、框选提供复制。补充任务、日志清理、设备主要操作及视频素材/录制/项目/草稿的反馈；运行报错与未保存共存，跳转沿用草稿保护。

修正原设计中的交互缺口：原生 title 只能看文字，改为溢出时出现可操作的紧凑补全区；分隔扣除自身 6px 后分配宽度，并记住 20%–80% 的比例；旧插件子页面卸载只清自己的消息，避免抹掉新页面状态。详情新增焦点处理和 Esc 关闭，便捷动作异常进入详情。

必要的表单校验、权限/覆盖/设备冲突确认和插件中心模态回执继续留在原处；未将所有 toast 或后台轮询机械转入底栏。真机输入、录制和所有第三方插件适配仍未新增实测。

本次前端全量 888 项通过，最后详情键盘调整后相关 18 项再次通过；生产构建通过，仍有既有 Console 大块警告。Chromium 使用真实组件和明确标注的模拟数据验证 800px 窗口无横向溢出、29px 高度、分界限位、补全区边界、实际复制和详情 Esc；没有连接设备。验收临时页面已移除。日志：`server/target/statusbar-audit-tests.log`、`statusbar-final-tests.log`、`statusbar-audit-build.log`。

## 后续调整：任务间距与市场/插件导航

- 任务工具条与列表增加 10px 间距，长列表在独立容器滚动。检查日志、模板、映射、视频素材/项目/草稿：已有 8–10px 分组间距，无同类零间距；设置为 14px，未机械改大或改小。
- 删除插件市场概览、插件功能选择器和整套管理弹窗之间的重复跳转。市场菜单直接展示插件市场或配置市场；顶部“插件”直接展示全部已安装插件，功能使用仍在工作台。
- 市场和已安装页增加名称/ID 搜索；市场“管理”带插件 ID 定位，直接点顶部“插件”清除该筛选。本地/URL 导入嵌入管理页，去掉重复大标题及导航末尾的加号入口。
- 配置市场去掉重复本地包清单，已装版本仍标在市场卡片上，提供“已安装配置”直达配置包页。安装、更新、权限与卸载协议保留，未修改后端。
- 插件卡片采用面板容器宽度换行，避免 1920px 视口下右栏很窄时仍横排挤压；间距保持紧凑，并简化面向用户的导入说明。

验证：前端全量 91 个文件、888 项通过；新增/调整导航直达、定向管理、搜索、导入及工作台过滤回归，原有管理防重复操作测试保留。生产构建通过，仍有既有大块体积警告。浏览器采用真实组件和模拟数据（当前会话未登录，未使用真实插件/设备）：1920×1080 下工具条至列表实测约 10px，列表内容 1460px 在 958px 容器内滚动；480px 窗口插件与配置市场无整体横向溢出，插件卡片纵向排布。实际检查市场搜索→管理定位、本地导入、URL 导入、配置市场；临时验收页已移除。日志保存在 `server/target/market-navigation-full-tests.log`、`market-navigation-build.log`。

## 最新调整：六个页面，管理与市场同页

按最新决定，主导航仅保留工作台、任务、日志、配置包、插件、设置。删除市场下拉及相关独立视图。首次无指定页面时默认工作台；已加载的可用功能自动打开，无插件则保持工作台空态。显式链接仍能进入对应任务/日志/插件功能，失效链接在插件发现完成后回工作台。

配置包页上方是已安装管理，导入/导出/新建/复制/删除平铺在工具栏，下方直接展示配置市场。插件页上方是已安装、下方是市场，共用搜索、刷新及操作状态；本地导入直接选文件，URL 导入只展开一行。两页均无二级分类页签，使用小标题、数量与细分隔线分区，宽屏多列卡片、窄屏单列，页面统一滚动。工作台原有功能页签排序保留。

配置包确认与详情仍由 Console 的单一弹窗宿主承载；管理页不重复挂弹窗。插件两区由同一个组件和快照管理，安装、卸载、启停后同时刷新，避免两个实例各自显示过期状态。没有变更后端安装/权限协议。

验证：全量 92 个文件、891 项通过；生产构建通过（既有大块体积警告保留）。真实 Chromium 组件验收采用明确标注的模拟数据：1920×1080 同屏看到插件两区，卡片约 374px；480px 时两页均无整体横向溢出，URL 输入展开后两区仍保留；配置新建只出现一个弹窗，取消可返回。未安装真实插件、未向设备发操作，临时验收页已移除。日志：`server/target/six-pages-full-tests.log`、`six-pages-build.log`。

## 最新调整：插件合并为统一列表

取消插件页“已安装 / 市场”两个区域，改为每个插件一张卡片。以插件 ID 合并本地和市场清单；市场同 ID 多版本取最新，本地未被市场收录的插件仍展示。卡片根据状态提供安装、启用/停用、更新、卸载，不再需要“管理”跳转。保留统一搜索、数量、刷新、本地和 URL 导入。

已安装版本、权限与状态仍来自管理快照，市场版本只用于更新比较；同版、更低版或执行形态不兼容不提供更新。管理读取失败时标明状态未确认并禁用依赖该状态的操作。卸载市场插件后原条目改为未安装，不重复、不丢失。

插件相关 21 项回归通过，覆盖去重、多版本、本地独有、卸载后状态变化和状态读取失败；生产构建通过，既有大块警告保留。未新增真实插件安装/卸载或真机验证。日志：`server/target/plugin-unified-tests.log`、`plugin-unified-build.log`。

## 最新调整：日志、配置包、插件视觉重整

本节替代前述管理页多列大卡片的视觉方案，六页导航、插件统一列表与配置包同页市场逻辑保持不变。

- 日志改为时间、级别、正文对齐的记录列表，降低分组装饰强度，正文保持高对比浅白。顶部增加当前记录搜索、暂停自动刷新和独立手动刷新，底部明确显示匹配数量及最近 200 条范围。保留设备/级别筛选和清空。新增请求顺序保护，修正旧筛选响应及清空后的在途请求覆盖问题，读取失败不再静默。
- 配置包采用当前上下文工具栏、本地配置列表、发现配置列表。名称与 ID 分开，补充版本、作者与使用状态；当前行用细黄边识别。市场与本地管理仍在一个页面，不增加二级页签。主操作和管理动作对齐，删除保持低强调。
- 插件由卡片网格改为横向列表，按名称/说明、状态/版本、操作对齐；宽屏名称与说明并排，窄屏分行。安装、启停和更新直接可达；权限、执行形态、历史版本、卸载归入一层详情展开。失败及依赖阻断保持外显，停用改为中性色。仍按 ID 合并市场与已安装，保留本地独有插件。

实施修正：验收发现配置包“新建”主按钮受工具栏透明背景覆盖，已补足主按钮颜色；实际计算结果为背景 `rgb(228,201,86)`、文字 `rgb(32,32,21)`。日志密集正文采用 13px / 1.65 行高，作为原 14px 通用正文规范的明确例外；完整规则同步至设计规范第 15 节。卸载多一次详情展开，换取常用动作区整洁，没有新增管理跳转。

验证：前端全量 **93 个文件、896 项测试通过**；生产构建通过，保留既有 Console 大块体积警告（627.02 KB，gzip 283.80 KB）。新增 3 项回归覆盖日志搜索与暂停轮询、筛选交错响应、读取失败及清空旧响应；相关页面定向 38 项通过。Chromium 使用真实 Vue 组件及明确标注的模拟数据，检查 1920×1080 的三页、详情展开、主按钮颜色，以及 480×800 三页换行；三个页面窄屏 body.scrollWidth 均为 480，无整体横向溢出，日志搜索“超时”显示 1 / 18 条。

边界：本轮未操作真实设备或安装/卸载真实插件；模拟页只验证布局与交互呈现，不作为后台安装链路验收。日志搜索范围仍是当前最近 200 条，没有增加服务端全历史搜索或分页。窄屏顶部导航为验收壳的可换行导航，本轮结论针对三个管理页内容，不扩展为全应用移动端验收。临时验收页面已移除。测试及构建日志：`server/target/management-redesign-full-tests.log`、`management-redesign-final-tests.log`、`management-redesign-build.log`。

## 最新调整：替换原生确认框

此前插件更新仍走 `globalThis.confirm`，与页面黑灰样式不一致。本次全量扫描前端业务代码，替换插件安装/更新/启用/停用/卸载、任务取消调度/删除、设备强制重连/删除/接管、脚本与函数删除/放弃编辑、配置包切换草稿保护和市场覆盖安装，以及 iframe 插件的确认桥入口。删除配置上下文中未使用的原生确认默认参数。其他已定制的表单、冲突、配置详情和覆盖弹窗沿用，不重造；未发现业务 alert/prompt 调用。

新增 App 单一确认宿主与按调用方作用域取消的异步确认方法。更新框显示插件名、ID、前后版本、来源、执行方式、请求权限、新增/移除权限与执行方式变化；长文本换行，正文独立滚动，按钮区固定。默认聚焦取消，Esc/遮罩/关闭均取消，Tab 约束与背景 inert 防止误操作。组件卸载/停用取消等待，重复请求不排队、不自动执行；确认后仍复核删除资源、设备和配置上下文，防止异步期间目标变化。配置切换先保持原选择，等待草稿确认后才实际切换。

验证：**95 个测试文件、903 项测试通过**，生产构建通过，保留既有 Console 大块警告。新增 7 项测试覆盖焦点/Esc/背景、纯文本与重复请求、关闭及作用域取消、不同调用方隔离、更新权限摘要与明确确认后才上传、插件页销毁取消、配置切换异步等待。Chromium 在 1920×1080 检查实际更新框（约 480×468px），在 480×800 检查长权限列表（框约 448×768px、正文滚动、操作区可见），无整体横向溢出；浏览器实际验证 Esc 取消与背景 inert。测试使用真实组件、模拟插件信息，没有实际更新/卸载插件或操作设备，临时页面已移除。

仍保留的原生界面只有浏览器刷新/关闭标签时的 beforeunload 未保存提醒，以及系统文件选择器；这两类由浏览器/系统管理，无法作为普通 Vue 弹窗定制。未修改安装权限协议、沙盒或 CSP。日志：`server/target/confirmation-full-tests.log`、`confirmation-dialog-tests.log`、`confirmation-build.log`。
